{
  "schema_version": 1,
  "platform": "linux",
  "rows": [
    {
      "tool": "apply_patch",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "ast_replace",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "ast_search",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "bash",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "applicable",
        "T5": "applicable",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "bash_kill",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "bash_status",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "bash_watch",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "bash_write",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "callgraph",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "conflicts",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "delete",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "edit",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "glob",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "grep",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "import",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "inspect",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "applicable",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "move",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "outline",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "powershell",
      "trajectories": {
        "T1": "n/a:platform",
        "T2": "n/a:platform",
        "T3": "n/a:platform",
        "T4": "n/a:platform",
        "T5": "n/a:platform",
        "T6": "n/a:platform",
        "T7": "n/a:platform"
      }
    },
    {
      "tool": "read",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "safety",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "search",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "applicable",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "status",
      "trajectories": {
        "T1": "n/a:host-catalog-transcript-no-model-callable-status-tool",
        "T2": "n/a:host-catalog-transcript-no-model-callable-status-tool",
        "T3": "n/a:host-catalog-transcript-no-model-callable-status-tool",
        "T4": "n/a:host-catalog-transcript-no-model-callable-status-tool",
        "T5": "n/a:host-catalog-transcript-no-model-callable-status-tool",
        "T6": "n/a:no-list-surface",
        "T7": "n/a:host-catalog-transcript-no-model-callable-status-tool"
      }
    },
    {
      "tool": "write",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "applicable",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    },
    {
      "tool": "zoom",
      "trajectories": {
        "T1": "applicable",
        "T2": "applicable",
        "T3": "n/a:no-permission-operation",
        "T4": "n/a:no-abortable-operation",
        "T5": "n/a:no-background-capability",
        "T6": "n/a:no-list-surface",
        "T7": "applicable"
      },
      "verdict_exclusions": {
        "T7": {
          "subject": "v1_host_process_exit",
          "issue": "https://github.com/anomalyco/opencode/issues/48340",
          "reason": "The pinned OpenCode 1 host serves the whole scenario and then never terminates inside a git repository, which every scenario project is, so the harness kills it at the row timeout. This row compares tool behaviour across the two host generations, and the served scenario answers that in full, so the V1 process exit and the kill that follows it are left out of the verdict. Serving is still required: both turns must be delivered and the tool result must arrive and match the other host, or the row fails."
        }
      }
    }
  ]
}
