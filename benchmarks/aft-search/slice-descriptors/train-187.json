{
  "slice_class": "engine_unwired",
  "targeted_mechanism": "none",
  "kind": "harness",
  "fixtures": [
    "crates/aft/tests/integration/tool_call_parity_test.rs"
  ],
  "slices": [
    "gh-shim-operator-label-rows",
    "health-test-work-count",
    "feature-config-slice-c",
    "timing-test-fixes"
  ],
  "note": "The only ranking-fence file touched is crates/aft/src/lib.rs, which gains module declarations for the setup plan, config fix and JSONC editor. No ranking, fusion, lane or truncation code changed, so the rows should be byte-equal to the reference. The train also adds the v14-gated gh-shim operator label rows, `aft setup --plan` and doctor --fix, and replaces three wall-clock test assertions with state checks."
}
