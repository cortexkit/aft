{
  "slice_class": "engine_unwired",
  "targeted_mechanism": "none",
  "kind": "harness",
  "fixtures": [
    "crates/aft/tests/integration/tool_call_parity_test.rs"
  ],
  "slices": [
    "oc2-permission-approve-hang",
    "sentinel-log-silent-gating",
    "bash-metadata-open-retry-yield",
    "configure-cancellation-mechanism-assertion"
  ],
  "note": "The only ranking-fence file touched is search_index.rs, and only its #[cfg(test)] git-probe stub: the signal it hands a caller changes from a started flag to an attempt counter so a test can assert cancellation stopped the probe ladder rather than that it finished quickly. No indexing, query, lane or ranking code is reached, so search rows are byte-identical to the reference (the predicate is the proof). The V2 permission ordering fix, the sentinel gating, and the bash metadata-open retry are all outside the fence."
}
