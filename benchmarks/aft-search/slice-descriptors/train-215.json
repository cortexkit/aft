{
  "slice_class": "engine_unwired",
  "targeted_mechanism": "none",
  "kind": "harness",
  "fixtures": [
    "crates/aft/tests/integration/tool_call_parity_test.rs"
  ],
  "slices": [
    "catalog-bash-companions",
    "session-kind-fixes",
    "session-kind-inventory",
    "umbrella-sessions-design",
    "search-recall-audit",
    "onnx-late-arrival",
    "drill-ux-polish",
    "delete-backup-budget"
  ],
  "note": "Ranking-fence files touched: the subc tool-schema artifact (catalog bash companions; aft_delete description), the benchmark-only recall audit (runs only with AFT_SEARCH_RECALL_AUDIT=1), semantic_index.rs and semantic_search/mod.rs for the ONNX Runtime late-arrival wait and the semantic-lane disclosure text, and trailer.rs plus the telemetry engine fixture: a complete exhausted page no longer prints a `shown N of N (walk)` trailer. No ranking, fusion, lane admission or truncation changed, so ranked rows should be byte-equal to the reference."
}
