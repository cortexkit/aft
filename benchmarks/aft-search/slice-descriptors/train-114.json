{
  "slice_class": "engine_unwired",
  "targeted_mechanism": "none",
  "kind": "harness",
  "fixtures": [
    "crates/aft/tests/integration/tool_call_parity_test.rs"
  ],
  "slices": [
    "semantic-bin-delta-persistence",
    "tier2-rollup-incremental-journal-spill-fleet-budget"
  ],
  "note": "Reference rebaselined on the slice-3 engine in this train (record-reference on a release build of d2d27fa96); the only fenced diff is the semantic.query_timeout_ms ceiling constant (15 s -> 30 s), byte-neutral where query embeds finish inside the budget, so rows are byte-equal to the new reference."
}
