{
  "slice_class": "engine_unwired",
  "targeted_mechanism": "none",
  "kind": "harness",
  "fixtures": [
    "crates/aft/tests/integration/tool_call_parity_test.rs"
  ],
  "slices": [
    "tier2-stale-not-building",
    "false-completion-footers",
    "watcher-seed-ecosystem-first",
    "semantic-index-fake-server-graceful-close"
  ],
  "note": "semantic_index.rs changes are confined to the #[cfg(test)] fake embedding servers (graceful connection close); no product line in the ranking fence moves, so search rows are byte-identical to the reference (the predicate is the proof). The tier2 health, bash completion wake, and watcher exclusion changes are outside the fence."
}
